1- Os relase info
   
   cat /etc/os-release 
   
2- User acounts 
   
   cat /etc/passwd | column -t -s : 
   cat /etc/shadow
   
3- Group Info 
   
   cat /etc/group 

4-Sudoers List
  
  cat /etc/sudores
  
4-Login information
  
  /var/log we can find all types of logs include btmp and wtmp 
  btmp saved info about failed login and wtmp keep hitrical data 
  of login 
  
  last -f /var/log/wtmp 
  
 
 5- Authentication Log
    Every user authntication on linux are logged in auth.log
    
    cat /var/log/auth.log
